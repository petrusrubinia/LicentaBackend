spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/licenta?useUnicode=yes&characterEncoding=UTF-8&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  main:
    allow-bean-definition-overriding: true
  #security:
  #  user:
  #   name: admin
  #    password: admin

server:
  port: 8080
  servlet:
    context-path: /api


